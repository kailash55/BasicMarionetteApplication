define([
    'jquery',
    'underscore',
    'backbone',
    'backbone.marionette'
], function( $, _, Backbone, Marionette) {
    'use strict';
    var DropdownModel = Backbone.Model.extend({

    })

    return DropdownModel
})